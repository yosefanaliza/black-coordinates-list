# Service A Configuration
SERVICE_B_HOST=service-b
SERVICE_B_PORT=8000
IP_API_TIMEOUT=5

# Service B Configuration
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_DB=0

# Logging Configuration
LOG_LEVEL=INFO

# Note: For local development with docker-compose, these values are already configured.
# For Kubernetes deployment, these are set in the deployment YAML files.
# For standalone local development, copy this file to .env and adjust as needed.
